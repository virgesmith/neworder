[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "pybind11>=2.6.0",
    "pytest"
]

[tool.pytest.ini_options]
minversion = "6.0"
testpaths = [
    "test"
]

build-backend = "setuptools.build_meta"
